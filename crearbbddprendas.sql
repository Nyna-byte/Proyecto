drop database prendas;
create database prendas;
use prendas;

create table camisa (n_fase varchar(3), descripcion_fase varchar(20), maquina varchar(20), puntada varchar(20), rpm varchar(10), ppc varchar(10), tc varchar(10), observaciones varchar(80))

//create table $nombre_prenda
